# GSD PLAN: 03-01-Voice-Loop

## Goal
Transform the simulated virtual room debate into an active voice-driven experience using Web Speech APIs.

## Requirements
- [x] Wire existing `useSpeechToText` hook into `DebatePage.jsx`.
- [x] Ensure microphone access requests and error states are handled gracefully.
- [x] Synchronize live transcript to real-time user captions.
- [x] Auto-trigger AI request upon manual microphone stop or significant pause.
- [x] Retain current high-fidelity Text-to-Speech flow for the AI responses.

## Execution Flow
1. Update `useSpeechToText` (ensure proper cleanup and lifecycle hooks). [COMPLETE]
2. Replace fake interval in `DebatePage` with hook activation. [COMPLETE]
3. Chain user audio confirmation to AI fetch lifecycle. [COMPLETE]
4. Verify and refine. [READY FOR USER TEST]
